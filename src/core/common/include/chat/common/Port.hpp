#pragma once

#include <cstdint>

namespace chat::common
{
/**
 * @brief A type to represent a port number used for networking.
 *
 * @details The values of a port number ranged from 0 to 65,535.
 */
enum class Port : std::uint16_t
{
};
}
